import React from "react"
import { Typography } from "@mui/material"

const Finished = () => (
    <Typography
      variant="h5"
      color="var(--primary)"
    >
      Task finished here
    </Typography>
  )
  
  export default Finished