import React from "react"
import { Typography } from "@mui/material"

const InProgress = () => (
    <Typography
      variant="h5"
      color="var(--primary)"
    >
      Tasks in progress here
    </Typography>
  )
  
  export default InProgress